{% extends 'admin_base.html.twig' %}

{% block title %}Edit GigImages{% endblock %}

{% block body %}
    <h1>Edit GigImages</h1>

    {{ include('gig_images/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('gigs_show',{id:gig_image.gig.id}) }}">back to list</a>

    {{ include('gig_images/_delete_form.html.twig') }}
{% endblock %}
