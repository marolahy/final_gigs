{% extends 'admin_base.html.twig' %}

{% block title %}GigImages{% endblock %}

{% block body %}
    <h1>GigImages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gig_image.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ vich_uploader_asset(gig_image, 'imageFile') }}" /></td>
            </tr>
            <tr>
                <th>Thumb</th>
                <td><img src="{{ vich_uploader_asset(gig_image, 'thumbFile') }}" /></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gigs_show',{id:gig_image.gig.id}) }}">back to list</a>

    <a href="{{ path('gig_images_edit', {'id': gig_image.id}) }}">edit</a>

    {{ include('gig_images/_delete_form.html.twig') }}
{% endblock %}
